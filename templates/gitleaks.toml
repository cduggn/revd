# Generated by revd. Owned by you — edit freely, revd will not overwrite it.
# Extends the built-in ruleset rather than replacing it.
[extend]
useDefault = true

# Example allowlist: add paths or regexes that produce known false positives.
[[allowlists]]
description = "test fixtures and generated files"
paths = [
  '''(^|/)testdata/''',
  '''(^|/)fixtures/''',
  '''\.snap$''',
]
